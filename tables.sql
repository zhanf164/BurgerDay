Create Table User (firstName, lastName, email, userName, password, optional=Group);

Create Table Preferences (user, dates);

Create Table reservations (user, reservations, date);

Create table photos (user, photo1, photo2, photo3);
